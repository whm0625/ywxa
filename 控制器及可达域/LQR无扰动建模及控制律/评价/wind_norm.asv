i = 1;
for t = 0:0.01:2
    if t<=0.7
        Vw(i)=-0.5-sin(1.43*pi*t);
    else
        Vw(i)=-0.5;
    end
    i = i+1;
end
Vw = Vw';

% 计算风扰在0-2s内的2范数
time_interval = 0:0.01:2;
wind_disturbance_interval = Vw(1:length(time_interval));
norm_p = norm(wind_disturbance_interval,5);

disp(['风扰在0-2s内的p范数为 ', num2str(norm_2)]);